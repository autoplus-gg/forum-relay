name: forum-relay

services:
  bot:
    container_name: forum-relay
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - ./config/.env
    ports:
      - "3000:3000"
    volumes:
      - ./config:/app/config:ro
      - ./.data:/app/.data
